// Parameters are passed by value. Copies of parameters are made.
// The actual variables that are passed as parameters are not affected.
f = function(x, y) {
        y[0] = x[0];
        return true;
    };
    
a = {1, 2, 3};
b;

// c depends on a, b
c = f(a, b);
