<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
         http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">
         
  <bean id="calculator" class="net.geocentral.dicta.HelloCalculator"/>
         
  <camelContext id="camelTest" xmlns="http://camel.apache.org/schema/spring">
    <route>
      <from uri="file://data/inbox1?noop=true"/>
      <to uri="bean:calculator?method=setVar(a, ${body})"/>
    </route>
    <route>
      <from uri="file://data/inbox2?noop=true"/>
      <to uri="bean:calculator?method=setVar(b, ${body})"/>
    </route>
    <route>
      <from uri="stream:in"/>
      <to uri="bean:calculator?method=getVar"/>
      <to uri="stream:out"/>
    </route>
  </camelContext>

</beans>
